pluginManagement {
    repositories {
        gradlePluginPortal()
        maven { url = uri("https://cache-redirector.jetbrains.com/plugins.jetbrains.com/maven") }
        mavenCentral()
    }
}
rootProject.name = "Blamely-intellij"
